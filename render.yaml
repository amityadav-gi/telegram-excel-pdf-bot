services:
  - type: worker
    name: telegram-excel-bot
    env: python
    buildCommand: |
      apt-get update && apt-get install -y libreoffice
      pip install -r requirements.txt
    startCommand: python bot.py